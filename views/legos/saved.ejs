<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="/saved.css">
</head>
<body>
    <h1>Saved Page</h1>

    Hi <%=username%>!
    <div class = "container">
        <% for(let i = 0; i < saved.length; i++){ %>
            <div class = "card">
                <a href = "/saved/<%=saved[i].id%>">
                    <%=saved[i].name%>
                </a>
                <a href = "/legos/<%=saved[i].series%>/series">
                    Series <%=saved[i].series%></a>
                    <img src = "<%=saved[i].img%>"><br/>
                    <form action="/saved/<%=saved[i].id%>?_method=DELETE" method="POST">
                        <input type="submit" value="Delete From My Collection">
                    </form>
                </div>
                <% } %>
            </div>

            <a href = "/legos">View Full Lego List</a>

            <form action="/sessions/destroy" method="post">
                <input type="submit" value="Log Out">
            </form>


        </body>
        </html>
